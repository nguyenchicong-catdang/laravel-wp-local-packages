{
  "name": "cloudflare-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "vite": "vite"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vite": "^7.3.1",
    "vitest": "~3.2.0",
    "wrangler": "^4.80.0"
  }
}
